source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "minima", "~> 2.5"
gem "jekyll-sitemap"
gem "jekyll-seo-tag"

# Renders LaTeX math to HTML/CSS at build time via KaTeX (ExecJS + Node.js),
# instead of relying on client-side KaTeX JS in the browser.
gem "kramdown-math-katex"

gem "webrick"
gem "csv"
gem "base64"
